{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pyinstaller",
            "type": "shell",
            "group": "build",
            "windows": {
                "command": "source \"./.venv/Scripts/activate\" && pyinstaller \"./src/main.py\" --noconfirm --clean --onefile --name=\"Kudan\" --icon=\"./icon.ico\" --add-data=\"./icon.ico:./\" --add-data=\"./res:./\" --windowed",
            },
        }
    ]
}
